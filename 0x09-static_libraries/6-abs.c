#include "main.h"
/**
 * _abs- to compute the absolute value of an integer
 * @z: the value to be checked
 * Return: value to be returned
 */

int _abs(int z)
{
	if (z >= 0)
	{
		return (z);
	}
	return (-z);
}

#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char cha;

	cha = 'a';
	while
		(cha <= 'z') {
			putchar(cha);
			cha ++;
		}
	putchar('\n');
	return (0);
}

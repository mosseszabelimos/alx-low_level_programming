#include "main.h"

/**
 * _isupper - check if letter is upper
 * @i: to be checked
 *
 * Return: 1 for the upper
 */

int _isupper(int i)
{
	if (i >= 65 && i <= 90)
	{
		return (1);
	}
	return (0);
}

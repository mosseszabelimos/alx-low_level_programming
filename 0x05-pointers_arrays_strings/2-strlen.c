#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 * @x: string value
 * Return: return the length
 */

int _strlen(char *x)
{
	int i;

	for (i = 0; x[i] != '\0'; i++)
	
	return (i);
}
